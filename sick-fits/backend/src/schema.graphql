"""Contains all of our queries and resolves. Cannot be left empty and since it's being referred to in the function createServer we need to define the resolvers of Mutation and Query"""
type Dog {
  name: String!
}

type Mutation {
  createDog(name: String!): Dog
}

type Query {
  dogs: [Dog]!
}